# 🚀 최종 서버 배포 가이드

## ✅ 확인: php_api_proxy는 더 이상 필요없습니다!

Firebase Functions로 완전히 전환했으므로 PHP 프록시 서버는 필요 없습니다.

---

## 📁 서버 `/public_html/` 폴더에 필요한 파일/폴더

### ✅ **build/** 폴더 전체 (17개 파일)

```
/public_html/                          (웹 루트)
├── index.html                        ← 필수
├── favicon.ico                       ← 아이콘
├── howtowork.png                     ← 이미지
├── logo.png                          ← 로고
├── asset-manifest.json               ← 자산 매니페스트
└── static/                           ← 정적 파일 폴더
    ├── css/
    │   ├── main.*.css
    │   └── main.*.css.map
    └── js/
        ├── main.*.js
        ├── main.*.js.map
        ├── main.*.js.LICENSE.txt
        ├── 455.*.chunk.js
        ├── 455.*.chunk.js.map
        ├── 977.*.chunk.js
        ├── 977.*.chunk.js.map
        └── 977.*.js.LICENSE.txt
```

**총 약 15-17개 파일, 크기: 약 17MB**

---

## ❌ **제거해야 할 파일/폴더**

서버에 있다면 모두 삭제하세요:

### PHP 프록시 관련
- ❌ `/public_html/php_api_proxy/` 폴더 전체
- ❌ `/public_html/php_api_proxy/api-proxy.php`
- ❌ `/public_html/php_api_proxy/load-env.php`
- ❌ `/public_html/php_api_proxy/analyze-problem-image.php`
- ❌ `/public_html/php_api_proxy/.env`
- ❌ `/public_html/php_api_proxy/.htaccess`

### 기타
- ❌ `.htaccess` (서버 루트에 있다면)
- ❌ `config.php`
- ❌ 기타 PHP 파일들

---

## ✅ **배포 방법**

### 1단계: 기존 파일 정리

```bash
# 서버 접속 후
cd /public_html/

# PHP 프록시 폴더 삭제
rm -rf php_api_proxy/

# 기존 빌드 파일 삭제 (선택사항, 백업 권장)
rm -rf static/
rm -f index.html asset-manifest.json
```

### 2단계: 새 파일 업로드

```bash
# 로컬에서
# build/ 폴더 전체를 압축하거나
# FTP/SFTP로 업로드

# 또는
cd build/
# 파일들을 /public_html/에 복사
```

### 3단계: 권한 설정

```bash
cd /public_html/
chmod 644 index.html
chmod 644 *.css
chmod 644 *.js
chmod 644 *.json
chmod 644 *.png
chmod 644 *.ico
```

---

## 🔍 **최종 확인 체크리스트**

배포 후 확인:

```
✅ /public_html/index.html 존재
✅ /public_html/static/css/ 폴더 존재
✅ /public_html/static/js/ 폴더 존재
✅ /public_html/asset-manifest.json 존재
❌ /public_html/php_api_proxy/ 삭제됨
❌ PHP 파일 없음 (.php 확장자)
```

---

## 🌐 **API 호출 흐름**

### 이전 (PHP)
```
브라우저 → php_api_proxy/api-proxy.php → OpenAI API
```

### 현재 (Firebase Functions)
```
브라우저 → Firebase Functions (openaiProxy) → OpenAI API
```

**중요:** Firebase Functions는 자동으로 Google Cloud에서 호스팅되므로 서버에 파일이 필요 없습니다!

---

## 📊 **서버 사용량 비교**

| 항목 | PHP 방식 | Firebase Functions |
|------|---------|-------------------|
| 서버 파일 | PHP 파일 필요 | 불필요 ✅ |
| 서버 용량 | php_api_proxy 폴더 | 0MB ✅ |
| 서버 설정 | 복잡 | 불필요 ✅ |
| 배포 | 수동 | 자동 ✅ |

---

## 🎯 **최종 정리**

### 서버에 있어야 할 것:
✅ **build/** 폴더 전체만!

### 서버에 없어도 되는 것:
❌ PHP 파일 전부
❌ 프록시 서버
❌ .env 파일
❌ config 파일

---

## 🧪 **테스트 방법**

배포 후 브라우저에서:

1. https://edgeenglish.net 접속
2. 개발자 도구 열기 (F12)
3. Console 탭 확인
4. 이미지 붙여넣기 시도
5. 에러가 없으면 ✅ 성공!

**예상되는 정상 로그:**
```
🤖 OpenAI 프록시 서버 호출 중...
✅ OpenAI API 응답 성공
```

**에러가 있다면:**
```
❌ Firebase Functions 로그 확인:
   firebase functions:log
```

---

**간단 정리: build/ 폴더만 서버에 업로드하면 됩니다! 🎉**

